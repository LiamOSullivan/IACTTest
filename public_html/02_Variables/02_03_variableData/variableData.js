/*************************
 * Foundations in Coding *       
 *************************
 *  IACT ** www.iact.ie  *
 *************************/

function setup(){
var count = 0;          // Declare an int named count, assigned the value 0
var letter = 'a';      // Declare a char named letter, assigned the value 'a'
var d = 132.32;      // Declare a double named d, assigned the value 132.32
var happy = false;  // Declare a boolean named happy, assigned the value false
var x = 4.0;          // Declare a float named x, assigned the value 4.0
               // Declare a float named y (no assignment)
                    }

